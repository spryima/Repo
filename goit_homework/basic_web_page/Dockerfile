FROM python:3.11.5

# Встановлюємо робочий каталог у контейнері
WORKDIR /app

# Копіюємо потрібний код в контейнер
COPY . .

# Вказуємо порт, на якому буде працювати застосунок
EXPOSE 3000

# Визначаємо команду для запуску застосунку
ENTRYPOINT ["python", "app/main.py"]